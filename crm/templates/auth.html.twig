{% extends 'base.html.twig' %}
{% block body %}
    <div class="container-fluid">
        <div class="row">
            <nav class="col-sm-3 col-md-2 d-none d-sm-block bg-light sidebar menu-nav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('accueil') }}">Tous les contacts</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('contact_new') }}">Ajouter un contact</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('logout') }}">Se déconnecter</a>
                    </li>
                </ul>

            </nav>
            <main class="col-sm-9 ml-sm-auto col-md-10 pt-3">{% block content %}{% endblock %}</main>
        </div>
    </div>
{% endblock %}